# Copy to `.env.local` in the project root (not committed to git).
#
# Demo login — comma-separated email:password pairs
# VITE_DEMO_CREDENTIALS=demo@example.com:your-password,other@example.com:other-password
#
# Demo phone login (optional, 10 digits)
# VITE_DEMO_PHONE=
